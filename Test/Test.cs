namespace CloudreveDesktop.Test;

public class Test
{
    public static void Tt()
    {
        // using var dokan = new Dokan(null!);
        // var rfs = new RfsService("/", "C");
        // var dokanBuilder = new DokanInstanceBuilder(dokan)
        //     .ConfigureOptions(options =>
        //     {
        //         options.Options = DokanOptions.DebugMode;
        //         options.MountPoint = "r:\\"; // 设置挂载到的硬盘
        //     });
        // using (dokanBuilder.Build(rfs))
        // {
        //     Console.ReadLine(); // 等待输入 ，实际应用中变成判断是否关闭程序
        // }
        //
        //
        // Console.WriteLine(@"Success");
        // Console.ReadLine();
        // Console.WriteLine(@"Success222");
    }
}